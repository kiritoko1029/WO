/**
 * User-facing release identity shared by the server health endpoints and the
 * desktop/web clients. Keep in sync with the workspace package.json versions
 * (enforced by test).
 */
export const APP_VERSION = '0.1.0';

/** Public source repository, also the only origin the desktop shell may open. */
export const SOURCE_REPOSITORY_URL = 'https://github.com/kiritoko1029/WO';
